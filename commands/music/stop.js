const { RichEmbed } = require("discord.js");
const { red } = require("../../colors.json");
const index = require("../../index.js");

module.exports = {
    
    config: {
        name: "stop",
        aliases: [],
        usage: "",
        description: "Stop listening to music.",
        accesibleby: "Members",
        category: "music"
    },

    run: async (bot, message, args) => {
        
    }
}